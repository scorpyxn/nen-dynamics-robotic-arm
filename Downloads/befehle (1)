Sql Installiert server aufsetzen
sudo apt-get update
sudo apt-get install apache2 libapache2-mod-php7.0 php7.0 php7.0-mysql mysql-server

Rechte für HTML Vz auf rwx für alle
sudo chmod 777 /var/www/html

Wo kommt was hin:
Die Webseite(mit php skripten und javascript skripten) mit der Steuerung kommt in das /var/www/html Vz 
das Python skript kommt auch da rein.

SQL Datenbank erstellt und gefüllt:
mit den Befehlen:

sudo mysql -u root -e "create database motoren";
sudo mysqladmin -u root password 123
mysql -u root -p
create table startpos (motor1 float, motor2 float, motor3 float, motor4 float, motor5 float, motor6 float); //ausgangsposition der motoren
create table pos (motor1 float, motor2 float, motor3 float, motor4 float, motor5 float, motor6 float); //aktuelle position
insert into startpos (motor1, motor2, motor3, motor4, motor5, motor6) values (1.5, 0.65, 1.5, 1.9, 0.85, 1.6) //startpositionen festlegen

python Mysql Erweiterung installieren:
sudo apt-get install python-mysqldb






